import React from 'react';
import { View, Text } from 'react-native';

function Tasks() {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text>Esta es la vista de Tasks</Text>
        </View>
    );
}

export default Tasks;
